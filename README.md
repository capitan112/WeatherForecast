# WeatherForecast
Swift app with weather forecast without 3rd party libraries

Used MVVM-C architecture without third part libraries

Project can run from WeatherForecast.xcodeproj

Used Result<> in respose.

Different types of tests.

